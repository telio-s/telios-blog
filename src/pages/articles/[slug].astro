---
import { getCollection } from "astro:content";
import { getLocalizedEntryPairs, isPublicLocalizedEntry } from "@/lib/localized-content";
import { getAbsoluteUrl } from "@/lib/seo";

export async function getStaticPaths() {
  const articles = getLocalizedEntryPairs(await getCollection("articles")).filter(isPublicLocalizedEntry);

  return articles.map((article) => ({
    params: { slug: article.en.data.translationKey },
    props: { redirectPath: `/en/articles/${article.en.data.translationKey}/` },
  }));
}

const { redirectPath } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="robots" content="noindex, follow" />
    <meta name="viewport" content="width=device-width" />
    <meta http-equiv="refresh" content={`0; url=${redirectPath}`} />
    <link rel="canonical" href={getAbsoluteUrl(redirectPath, Astro.site ?? Astro.url)} />
    <title>Redirecting to article</title>
  </head>
  <body>
    <p><a href={redirectPath}>Continue to article</a></p>
  </body>
</html>
